# View results
head(ego)
# Check if results exist
if (!is.null(ego) && nrow(as.data.frame(ego)) > 0) {
library(enrichplot)
library(ggplot2)
dotplot(ego, showCategory = 10) +
ggtitle("Top 10 Enriched GO Terms")
} else {
message("No significant GO terms found. Try relaxing cutoffs.")
}
# Install package
# BiocManager::install(c("enrichplot"))
# Load library
library(enrichplot)
library(clusterProfiler)
# Dotplot visualization for GO enrichment results
dotplot(ego, showCategory = 10) + ggtitle("Top 10 Enriched GO Terms")
# Barplot visualization
barplot(
ego,
showCategory = 10,
drop = TRUE,
x = "Count"
) +
ggtitle("Top 10 Enriched GO terms - Bar Plot")
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[logFC > 1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "BH",
qvalueCutoff = 0.1)
ego
# View results
head(ego)
# Check if results exist
if (!is.null(ego) && nrow(as.data.frame(ego)) > 0) {
library(enrichplot)
library(ggplot2)
dotplot(ego, showCategory = 10) +
ggtitle("Top 10 Enriched GO Terms")
} else {
message("No significant GO terms found. Try relaxing cutoffs.")
}
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[logFC > 1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "BH",
qvalueCutoff = 0.5)
ego
# View results
head(ego)
# Check if results exist
if (!is.null(ego) && nrow(as.data.frame(ego)) > 0) {
library(enrichplot)
library(ggplot2)
dotplot(ego, showCategory = 10) +
ggtitle("Top 10 Enriched GO Terms")
} else {
message("No significant GO terms found. Try relaxing cutoffs.")
}
# Install package
# BiocManager::install(c("enrichplot"))
# Load library
library(enrichplot)
library(clusterProfiler)
# Dotplot visualization for GO enrichment results
dotplot(ego, showCategory = 10) + ggtitle("Top 10 Enriched GO Terms")
# Barplot visualization
barplot(
ego,
showCategory = 10,
drop = TRUE,
x = "Count"
) +
ggtitle("Top 10 Enriched GO terms - Bar Plot")
# Install package
# BiocManager::install(c("enrichplot"))
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[logFC > 1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "BH",
qvalueCutoff = 0.5)
# Load library
library(enrichplot)
library(clusterProfiler)
# Dotplot visualization for GO enrichment results
dotplot(ego, showCategory = 10) + ggtitle("Top 10 Enriched GO Terms")
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[logFC > 1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "BH",
qvalueCutoff = 0.5)
ego
# View results
head(ego)
# Check if results exist
if (!is.null(ego) && nrow(as.data.frame(ego)) > 0) {
library(enrichplot)
library(ggplot2)
dotplot(ego, showCategory = 10) +
ggtitle("Top 10 Enriched GO Terms")
} else {
message("No significant GO terms found. Try relaxing cutoffs.")
}
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[logFC > 1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "BH",
qvalueCutoff = 0.5)
ego
# View results
head(ego)
# Check if results exist
if (!is.null(ego) && nrow(as.data.frame(ego)) > 0) {
library(enrichplot)
library(ggplot2)
dotplot(ego, showCategory = 10) +
ggtitle("Top 10 Enriched GO Terms")
} else {
message("No significant GO terms found. Try relaxing cutoffs.")
}
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[logFC > 1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "BH",
qvalueCutoff = 0.5)
ego
# View results
head(ego)
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[logFC > 1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "BH",
qvalueCutoff = 0.5)
ego
# View results
head(ego)
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[logFC > 1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "BH",
qvalueCutoff = 0.5)
ego
# View results
head(ego)
# Load library
library(enrichplot)
library(clusterProfiler)
# Check if results exist
if (!is.null(ego) && nrow(as.data.frame(ego)) > 0) {
library(enrichplot)
library(ggplot2)
dotplot(ego, showCategory = 10) +
ggtitle("Top 10 Enriched GO Terms")
} else {
message("No significant GO terms found. Try relaxing cutoffs.")
}
library(biomaRt)
library(clusterProfiler)
library(AnnotationDbi)
library(biomaRt)
library(clusterProfiler)
library(AnnotationDbi)
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[logFC > 1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "BH",
qvalueCutoff = 0.5)
ego
# View results
head(ego)
# Load library
library(enrichplot)
library(clusterProfiler)
# Check if results exist
if (!is.null(ego) && nrow(as.data.frame(ego)) > 0) {
library(enrichplot)
library(ggplot2)
dotplot(ego, showCategory = 10) +
ggtitle("Top 10 Enriched GO Terms")
} else {
message("No significant GO terms found. Try relaxing cutoffs.")
}
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[logFC > 1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "BH",
qvalueCutoff = 0.8)
ego
# View results
head(ego)
# Load library
library(enrichplot)
library(clusterProfiler)
# Check if results exist
if (!is.null(ego) && nrow(as.data.frame(ego)) > 0) {
library(enrichplot)
library(ggplot2)
dotplot(ego, showCategory = 10) +
ggtitle("Top 10 Enriched GO Terms")
} else {
message("No significant GO terms found. Try relaxing cutoffs.")
}
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[logFC > 1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "none",
qvalueCutoff = 0.05)
ego
# View results
head(ego)
# Load library
library(enrichplot)
library(clusterProfiler)
# Check if results exist
if (!is.null(ego) && nrow(as.data.frame(ego)) > 0) {
library(enrichplot)
library(ggplot2)
dotplot(ego, showCategory = 10) +
ggtitle("Top 10 Enriched GO Terms")
} else {
message("No significant GO terms found. Try relaxing cutoffs.")
}
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[logFC > 1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "none",
qvalueCutoff = 0.5)
ego
# View results
head(ego)
# Load library
library(enrichplot)
library(clusterProfiler)
# Check if results exist
if (!is.null(ego) && nrow(as.data.frame(ego)) > 0) {
library(enrichplot)
library(ggplot2)
dotplot(ego, showCategory = 10) +
ggtitle("Top 10 Enriched GO Terms")
} else {
message("No significant GO terms found. Try relaxing cutoffs.")
}
View(annotations)
View(mart)
logFC
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[abs(logFC) > 0.1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "none",
qvalueCutoff = 0.5)
ego
# View results
head(ego)
# Load library
library(enrichplot)
library(clusterProfiler)
# Check if results exist
if (!is.null(ego) && nrow(as.data.frame(ego)) > 0) {
library(enrichplot)
library(ggplot2)
dotplot(ego, showCategory = 10) +
ggtitle("Top 10 Enriched GO Terms")
} else {
message("No significant GO terms found. Try relaxing cutoffs.")
}
# Install package
# BiocManager::install(c("enrichplot"))
# Dotplot visualization for GO enrichment results
dotplot(ego, showCategory = 10) + ggtitle("Top 10 Enriched GO Terms")
# Barplot visualization
barplot(
ego,
showCategory = 10,
drop = TRUE,
x = "Count"
) +
ggtitle("Top 10 Enriched GO terms - Bar Plot")
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[abs(logFC) > 0.1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "BH",
qvalueCutoff = 0.05)
ego
# View results
head(ego)
# Load library
library(enrichplot)
library(clusterProfiler)
# Check if results exist
if (!is.null(ego) && nrow(as.data.frame(ego)) > 0) {
library(enrichplot)
library(ggplot2)
dotplot(ego, showCategory = 10) +
ggtitle("Top 10 Enriched GO Terms")
} else {
message("No significant GO terms found. Try relaxing cutoffs.")
}
# Install package
# BiocManager::install(c("enrichplot"))
# Dotplot visualization for GO enrichment results
dotplot(ego, showCategory = 10) + ggtitle("Top 10 Enriched GO Terms")
# Barplot visualization
barplot(
ego,
showCategory = 10,
drop = TRUE,
x = "Count"
) +
ggtitle("Top 10 Enriched GO terms - Bar Plot")
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[abs(logFC) > 0.1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "BH",
qvalueCutoff = 0.05)
ego
# View results
head(ego)
# Load library
library(enrichplot)
library(clusterProfiler)
# Check if results exist
if (!is.null(ego) && nrow(as.data.frame(ego)) > 0) {
library(enrichplot)
library(ggplot2)
dotplot(ego, showCategory = 10) +
ggtitle("Top 10 Enriched GO Terms")
} else {
message("No significant GO terms found. Try relaxing cutoffs.")
}
# Load library
library(org.Hs.eg.db)
# Sample data
gene_list <- c(annotations$external_gene_name) # Replace with your gene list
gene_list
# Simulated logFC values for the gene list (normally you would get these from
# your analysis).
logFC <- rnorm(length(gene_list))
logFC
names(logFC) <- gene_list
names(logFC)
# Enrichment analysis for GO terms
ego <- enrichGO(gene = names(logFC)[abs(logFC) > 0.1], OrgDb = org.Hs.eg.db,
keyType = "SYMBOL", ont = "BP", pAdjustMethod = "BH",
qvalueCutoff = 0.05)
ego
# View results
head(ego)
# Load library
library(enrichplot)
library(clusterProfiler)
# Check if results exist
if (!is.null(ego) && nrow(as.data.frame(ego)) > 0) {
library(enrichplot)
library(ggplot2)
dotplot(ego, showCategory = 10) +
ggtitle("Top 10 Enriched GO Terms")
} else {
message("No significant GO terms found. Try relaxing cutoffs.")
}
setwd("D:/Ajay Files/R Programming for Bioinformatics/Chapter 10")
setwd("D:/Ajay Files/R Programming for Bioinformatics/Chapter 11")
